```
### bundler => webpack 打包文件核心代码 
  抽象语法树 概念  通过它可以读取文件代码
  webpack打包原理 => 通过babel提供的方法能得到抽象语法树，
  解析语法树得到文件代码的依赖路径（包含依赖的绝对路径和相对路径）,
  再通过递归取出依赖下的依赖和代码 并返回出去。
  同时构建es5不支持的require()函数进行编译进而实现打包出浏览器能识别的代码。  

```